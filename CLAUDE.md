# CLAUDE.md - 工作规则

## 核心原则
- **不询问，直接执行最合理方案**
- **每个文件修改完立刻验证**
- **遇到错误先尝试自动修复，修不了才上报**
- **上下文快满时主动总结写入 progress.md，然后继续**

## 汇报模板（每完成一步必须输出）
```
1. 做了什么: xxx
2. 验证结果: 命令输出/截图
3. 下一步: xxx
```
不输出视为未完成。

## 文件修改流程
1. 改代码 → 立刻编译验证 (`npx tsc`)
2. 有错 → 自动修复
3. 编译通过 → 立刻功能测试
4. 测试通过 → 记录到 progress.md → 继续下一步

## 报错处理流程
1. 记录错误信息
2. 列出已尝试的修复方案（至少2个）
3. 选最保守的方案执行
4. 修好了 → 验证 → 继续
5. 修不好 → 记录到 progress.md 已知问题 → 继续

## wrangler dev 调试规则
- 改 src/ 后必须 `source ~/.zshrc && npx tsc` 重新编译到 dist/
- 不要依赖 wrangler 热重载，它经常不工作
- 调试用 `return c.json({debug: true, ...})` 而不是 console.log

## 上下文管理
- 每次交互结束前更新 progress.md
- 上下文超过 80% 时主动总结并写入 progress.md
- 长任务用子代理执行，主代理监控进度
