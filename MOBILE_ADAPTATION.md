# 移动端适配说明

## 概述
为剑侠情缘 Web 版游戏添加了完整的移动端响应式适配方案。

## 功能特性

### 1. 响应式检测
- 使用 `useIsMobile` Hook 自动检测设备类型
- 默认断点：768px（可配置）
- 实时响应窗口大小变化

### 2. 移动端布局

#### 顶部导航栏
- 固定在页面顶部
- 包含功能：
  - 竞技按钮
  - 排行按钮
  - 帮助按钮（点击显示 LeftPanel 内容）

#### 主游戏区域
- 全屏显示游戏内容
- 左右侧边栏自动隐藏
- 游戏地图自适应屏幕宽度

#### 右下角悬浮按钮
- 圆形悬浮按钮（☰ 图标）
- 点击弹出建筑列表（RightPanel 内容）
- 固定在右下角，不遮挡主要内容

#### 底部功能导航栏
- 固定在页面底部
- 包含所有主要功能图标：
  - 内政、军事、外交、科技
  - 武将、物品、商城、市场
- 横向滚动支持更多功能

### 3. 抽屉组件（MobileDrawer）

#### 左侧抽屉
- 显示 LeftPanel 内容
- 包含：用户信息、服务器信息、快捷链接
- 从左侧滑入

#### 右侧抽屉
- 显示 RightPanel 内容（建筑列表）
- 包含：所有建筑的详细信息
- 从右侧滑入

#### 交互特性
- 点击遮罩层关闭
- 点击关闭按钮（✕）关闭
- 平滑动画过渡

## 文件结构

```
src/features/webgame/
├── components/
│   ├── JxWeb.tsx                 # 主组件（已添加移动端适配）
│   ├── MobileDrawer.tsx          # 移动端抽屉组件
│   ├── LeftPanel.tsx             # 左侧面板内容
│   └── RightPanel.tsx            # 右侧面板内容
├── hooks/
│   └── useIsMobile.ts            # 移动端检测 Hook
└── styles/
    ├── mobile.module.css         # 移动端样式
    └── mobileDrawer.module.css   # 抽屉组件样式
```

## 使用方式

### PC 端
- 保持原有布局不变
- 左侧面板、顶部导航、右侧面板正常显示

### 移动端
- 自动切换到移动端布局
- 侧边栏隐藏，通过按钮/抽屉访问
- 底部导航栏替代顶部功能按钮

## 技术实现

### 响应式断点
```typescript
const isMobile = useIsMobile(768); // 768px 以下为移动端
```

### 条件渲染
```tsx
{isMobile && <MobileTopNav />}
{!isMobile && <DesktopSidebar />}
```

### CSS 类名控制
```tsx
className={isMobile ? mobileStyles.hiddenOnMobile : ''}
```

## 样式特性

- 使用 CSS Modules 避免样式冲突
- 支持深色/浅色主题（古风配色）
- 平滑动画过渡效果
- 触摸友好的按钮尺寸

## 兼容性

- ✅ iOS Safari
- ✅ Android Chrome
- ✅ 微信内置浏览器
- ✅ 现代桌面浏览器

## 后续优化建议

1. 添加手势支持（滑动关闭抽屉）
2. 优化触摸区域大小
3. 添加横屏模式适配
4. 性能优化（虚拟滚动）
5. PWA 支持（离线访问）

## 测试清单

- [ ] 移动端顶部导航功能正常
- [ ] 帮助按钮打开左侧抽屉
- [ ] 右下角按钮打开建筑列表
- [ ] 底部导航栏功能正常
- [ ] 抽屉关闭功能正常
- [ ] 游戏主界面全屏显示
- [ ] 响应式切换流畅
- [ ] 触摸交互友好
